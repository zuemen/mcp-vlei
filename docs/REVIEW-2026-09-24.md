# mcp-vlei 全庫審閱（2026-09-24）

## 處理狀態（同日更新，未 commit）

**已修，且有測試：**
- **P0 #1–#6 全部**：簽章改從 witness 的 KEL 取 key state（`kel.py`，新）；簽署者必須是持有人，或持有人經 KEL 錨定的委派 AID；每張憑證的發行都要錨定在發行者 KEL（`chain.verify_issuance`）；全鏈查撤銷且要求 `iss`；leaf 型別依 schema 判斷。
- **新發現並已修**：ECR 的 LEI 沒綁 LE（QVI 可繞過 LE 直接發 ECR，LE 也可替別家公司的 LEI 發）。由 skill 生成的 server 發現，修在 `chain.verify_vlei_chain`。
- **P1 順帶修掉的**：`unknown_root` 在真實路徑能正確觸發；ReplayCache 改成驗章後才記錄；沒有時區的 ts、壞掉的 base64 都會回具名的失敗層；簽章檢查不再能被跳過。
- **Demo**：console 六幕全部真跑（詳見 `examples/console/README.md`）；`test_acceptance.py` 語法錯誤已修，並新增 live 冒名測試 `test_2c`；閘道路徑改走套件的 `verify_call`，filing-server 的 ctx 注入 bug 已修，agentgateway 鎖在 v1.5.0，並加了 `.dockerignore`。
- **營運**：association-server 預設改綁 127.0.0.1；bootstrap 的 `fail` 改輸出到 stderr；重發 SAID 改成比對 `vc create` 前後的清單（原本 `tail -1` 會撿到剛撤銷的那張）；新增 `--reissue`；witness host 埠可在 `scripts/.env` 設定（本機 Windows 把 5550–5649 保留了）；`record-check.sh` 原本的 SSE 檢查因 `pipefail` 永遠失敗，已修，並新增場景 4、5 的檢查。
- **文件**：SPEC 新增「Whose key, and who may sign」一節；CONFORMANCE 新增第 17–22 列與缺陷記錄；skill 的驗證表與矛盾處已修；測試數已同步；CI 新增 examples job；在 PR #1、#2 上實際跑過，三個 job 都通過。

**驗證結果**：
- 離線：套件 128、console 18、regulator 32、skill-server 31，全綠。
- live（重建後的 keripy 堆疊）：驗收 7/7，含冒名測試。
- 經過 gateway 的冒名呼叫，回 `invalid_signature`。
- 錄影流程 0→5，含撤銷與重發，全部符合預期。

**第二輪（同日）已修，且有測試：**
- **P1 全部**：
  - attestation 改用 witness 上的 key state 驗證，並要求簽的人是已驗證的伺服器（或其委派）、內容講的是自己；不擋未來時間的問題也修了。
  - `verify_server=True` 卻沒給 roots 時直接報錯。
  - 零參數工具的 digest 不再對不上（改前改後都測過）。
  - extension 沒 bind、也沒給 requirements 時，改成 fail closed。
  - 選了 verifier 來源卻沒給 verifier 時，直接報錯。
  - `VleiVerifier` 會比對 SAID，快取改用 `(aid, said)` 當 key。
  - scope 比對型別嚴格化。
  - report 的每一項都標出是否 `skipped`。
  - 套件測試從 128 增加到 143。
- **`examples/my-agent/agent.py`**：
  - 改用 SDK 2.2.0 的 `Client` 並宣告 extension，簽章改走 `kli sign`。
  - 修正 `input_schema`、`meta`、`is_error` 的欄位名。
  - 用照劇本回應的假模型，對 live association-server 實際跑通。
- **bootstrap 那行 `ecr: command not found`**：原因是我在它背景執行途中改了腳本（bash 邊讀邊跑），不是腳本本身的 bug，之後的執行都沒有再出現。
- **套件中繼資料**：pyproject 的版本改為 0.2.0，作者欄與 CITATION 一致；`CommandSigner` 補進匯出清單。

**第三輪（同日）已修，且有測試：**
- **Duplicity 偵測**：`WitnessKeyStates` 可同時查多個 witness，逐一比對各份 KEL 的事件。
  - 兩個 witness 在同一個序號給出不同的事件，一律拒絕。
  - 只是落後的 witness（副本較短）不算 duplicity。
  - 回應的 witness 數量不到法定數時，視為 key state 無法確立。
  - 設定方式：`VleiIdentity(witness_urls=…)` 或環境變數 `VLEI_WITNESS_URLS`。
  - live 驗證：五個 AID 在三個 witness 上完全一致；驗收 7/7 和錄影流程 0→5 都在三 witness 比對下通過。
  - 限制：witness 若都由同一方營運，仍可能被串通成一致，這點已寫進 CONFORMANCE。
- **時間戳改為毫秒**：同一秒內兩次相同的合法呼叫，原本會被當成 replay。
- **`REPORT.md`**：21 個缺口都標出處理方式（已修／決定不修並附理由／已知限制／待修）。
- **測試數**：套件 150、console 18、regulator 32、skill-server 31。

**決定不做：撤銷單一 agent 的委派。** KERI 沒有只靠委派者就能單方撤銷已委派 AID 的機制：委派者只能拒絕批准它之後的輪換，現有金鑰仍然有效。要做到這件事，必須另外發一張可撤銷的「agent 授權憑證」（ACDC），由 TEL 管理，等於新增一種憑證類型。這屬於設計層級的決定，應該等 GLEIF 回覆委派模型的問題之後再定，也就是第 19 頁三個請求之一。

**仍未處理：**
- `examples/skill-server/REPORT.md` 沒寫入：harness 擋了 subagent，內容在它的回報裡，等使用者決定。
- **限制**：不偵測 duplicity（只問一個 witness）；規格中「撤銷委派」這個開關沒有實作。
- **簡報（Prompt 1）** 還沒重新產生，`build-deck.py` 的文字已更新。
- 文件不一致與上游 live 重現：由背景 subagent 處理中，結果見其回報。

## 現況

- 套件測試：`pytest packages/mcp-vlei/tests` → **77 passed**（68 個 `def test_`，含 10 組參數化）。CI 最近 8 次全綠，但 **CI 只跑 package 測試**。
- `examples/association-server/tests/test_acceptance.py` **SyntaxError**（:94 f-string 內含實際換行；:189-191、:226-228 縮排錯），自 `c5d5bef` 起無法載入。README / examples/README 仍宣稱 acceptance 綠。
- 本機最後一次 `bootstrap.log`（09-23 17:44）中途無聲中斷（無 LE、無 Done）；前一輪 acceptance 已呼叫 `/api/revoke`。**`credentials/env.json` 指向的 ECR 很可能已撤銷**——錄影前先跑 `scripts/reset-demo.sh`，再用 `record-check.sh` 確認。

## P0：直接削弱論點（「vLEI 讓冒名不可行」）

| # | 問題 | 位置 | 已實跑 |
|---|---|---|---|
| 1 | 請求簽章用**呼叫者自帶**的 `_meta["org.gleif.vlei/verkey"]` 驗，未從 KEL 解析 AID key state，也不比對 signer 與 issuee／delegatedAid。拿到任何一張 ECR（每次呼叫都會送給對方伺服器）即可用自己的金鑰冒用 | `extension.py:278` | ✅ 攻擊者金鑰→工具執行 |
| 2 | 不帶 verkey 時 freshness／digest／signature 三項標 skipped **但照樣放行**；report 的 `skipped()` 又記成 `passed=True` | `extension.py:303-305`、`report.py:139` | ✅ 垃圾簽章→工具執行 |
| 3 | delegation 檢查無條件通過 | `extension.py:307-311` | — |
| 4 | holder 在解析失敗時退回呼叫者宣稱的 `signature.aid`，違反 SPEC:120-124 MUST NOT | `extension.py:272` | — |
| 5 | 不檢查憑證類型／schema；未帶 `credentialSaid` 時 `_said_of` 取串流第一個 `d`（QVI） | `extension.py:266,355-372,408-416`、`chain.py` | ✅ QVI 當 ECR 過關 |
| 6 | 撤銷只查葉節點；LE／QVI 被撤不會發現；`_has_revocation` 只看 `rev` 不要求 `iss`，TEL 回應未驗簽 | `extension.py:336`、`revocation.py:97,111-114` | 部分 |

SPEC.md:183 自己要求「under the presented AID's current key state」，docs/CONFORMANCE.md 的「未宣稱」段沒有揭露 1–3。

## P0：Demo 真實性

- Console 場景 4（經閘道）、5（依 skill 生成的 server）只是場景 1 換標籤，仍在同 process 呼叫 `VleiIdentity._verify`；「git diff — no output」是寫死字串（`examples/console/app.py:186-190`）。與 DEMO-CONSOLE.md「畫面無手寫內容」矛盾。
- 場景 3 按下即把 stub TEL 設 revoked（`app.py:430-433`），並非真撤銷後重驗；與 DEMO.md:172-176 的操作流程不符。
- `examples/my-agent/agent.py:27` `from mcp.client.http import ...` 在 SDK 2.2.0 → ModuleNotFoundError；:99 用裸 `ClientSession`（README 自己說這樣拿不到 extension）；:103 需要的 `<aid>.key` 沒有腳本產生。
- 閘道路徑（讀碼判斷，未實跑）：filing-server `ctx: Any` 不會被 SDK 注入 → 受保護工具恆 PermissionError；vlei-authz :158-166 用 delegatedAid 查 verifier（與「問 issuee」原則相反）；只在有 `x-vlei-verkey` header 時才驗簽，而 agent 把 verkey 放在 `_meta` → 實際不驗簽。

## P1：套件其他缺陷

- `unknown_root` 在真實路徑不會出現：`walk_chain` 丟 `ChainInvalid`（`chain.py:200-205`），測試只靠 stub 通過。skill 依賴此層名。
- ReplayCache 在驗簽**之前**記錄（`signing.py:354-356` 早於 :359/:368）→ 偽造請求可污染快取讓合法請求被判 replay；未認證請求可讓 `_seen` 無上限成長。
- 格式錯誤輸入拋非 VleiError（無時區 ts → TypeError `signing.py:346`；壞 base64 → binascii.Error；缺 `d` → KeyError `chain.py:122`；scope 型別不符 → TypeError，字串被當字元集合 `signing.py:404-405`），違反 SPEC:178-179「錯誤須寫出失敗層」。
- `attest.py:122-123` 不擋未來時間（10 年後的 `verifiedAt` 被接受）。
- 客戶端驗 attestation 用的是**伺服器自己宣稱**的 verkey（`client.py:343`），也沒傳 `expected_subject_aid`。
- `verify_server=True` 但沒給 `accepted_roots` 時完全不驗伺服器（`client.py:227-228`），README:48-51 的範例正是這個設定。
- 零參數受保護工具必定 `digest_mismatch`（client 簽 `{}`，SDK 送 None）。
- 未呼叫 `bind()` 時所有工具變公開；`revocation_source="verifier"` 未給 verifier 時靜默略過撤銷。
- `VleiVerifier` 快取只以 aid 為 key。
- 測試未覆蓋：缺 verkey／自帶 verkey、真實 `unknown_root`、`TelRevocationChecker`、`VleiVerifier` HTTP、`VleiClient` 全部、未 bind、格式錯誤輸入。

## 營運／安全

- `association-server/server.py:176` 綁 `0.0.0.0`，`/api/revoke`（:139-166）無驗證。`server.log` 顯示 8080 曾被多個外部 IP 掃描；`/api/revoke` 只被 127.0.0.1 呼叫過。建議改綁 127.0.0.1。
- 無 `.dockerignore`，`Dockerfile.python:9` 的 `COPY examples` 會把 keys 目錄打進映像；agentgateway 映像用 `:latest`。
- bootstrap 的 `fail` 在 `$(issue …)` 子殼內（:368），錯誤被吞、靜默退出——與 README「fail loudly」不符，本機那次中斷就是這樣。`vc list | tail -1` 取 SAID 重跑時可能拿錯（:365）。

## 規格／文件不一致

- **capability 位置**：SPEC.md:11,56、schema.ts:11,50、discover-response.json:10、workflow.md:38、DEMO.md:403 寫在 `Implementation.extensions`；SDK 2.2.0 的 `Implementation` 沒有此欄位，實際在 `ServerCapabilities/ClientCapabilities.extensions`（SKILL.md 與 SPEC.md:231 又寫對）。
- `-32021` 的 `requiredCapabilities`：schema.ts:211-214 仍是 `string[]`，SPEC/程式是物件；implementing CONFORMANCE.md:60 宣稱「四處皆已修」。
- `missing_credential` 層與 `verkey`／`credentialSaid`／`failure`／`report` meta key 未進 SPEC 表與 schema.ts；error-responses.json:54 的 `failureLayer` 程式不會輸出。`ExtensionRequired` 從未被 raise，`-32021` 未實作。
- 驗證順序三個版本（SKILL.md:160-170／report.py:40-47＋slide 10／workflow.md:57-65）；各處說「revocation last」但其後還有 authority。
- SPEC.md:238-240 說有 Claude Desktop 連線測試，實際用 SDK `Client`。
- 數字：README:58「67 tests」→ 77；CONFORMANCE.md:13 test_chain 12 → 15；錄影長度 3:35 vs 場景合計 4:05；GOVERNMENT.md:65「Five stages」實列 0–5 共六；閘道 header 數 2／3／4 各處不同；DEMO.md:284「Slide 8」實為 13；DEMO-CONSOLE.md:120 按鍵 1–6 實為 0–5；pyproject 版本 0.1.0 vs 文件 v0.2、作者欄不一致；`docs/media/` 不存在；SPEC.md:78「来」為簡體。
- impersonation 的 edge_test 中「被擋的 3 組」是 **Python SDK client 端 pydantic** 擋的（edge_test.py:64），直接送 raw JSON-RPC 可繞過——上台勿說成「協定會驗型別」。

## 建議處理順序

1. 修 P0 #1–#4：從 KEL（或至少 verifier 回傳的 key state）取 signer 公鑰；要求 signer == issuee，或為 issuee 經 KEL 委派的 AID；缺簽章即拒；`skipped` 不得記為 passed。補「自帶 verkey」「無 verkey」「別人的 ECR」三個負向測試。
2. 修 P0 #5–#6：比對 requirement 的 credential 類型與 schema SAID；沿鏈每一層查撤銷。
3. 修 `test_acceptance.py` 語法並把 console 測試（不需容器）加進 CI。
4. Console 場景 3/4/5 改為真跑（或在畫面與台詞明示為模擬），否則與「無手寫」說法衝突。
5. 文件數字與 capability 位置統一；`association-server` 改綁 127.0.0.1。
6. 錄影前 `reset-demo.sh` → `record-check.sh`。
